{
  "name": "dude-blocks",
  "version": "1.0.0",
  "description": "Native Gutenberg blocks for Dude theme",
  "scripts": {
    "build:pricing-hero": "cd pricing-hero && wp-scripts build src/index.js --output-path=build",
    "build:pricing-cta": "cd pricing-cta && wp-scripts build src/index.js --output-path=build",
    "build": "npm run build:pricing-hero && npm run build:pricing-cta",
    "start:pricing-hero": "cd pricing-hero && wp-scripts start src/index.js --output-path=build",
    "start:pricing-cta": "cd pricing-cta && wp-scripts start src/index.js --output-path=build"
  },
  "devDependencies": {
    "@wordpress/scripts": "^28.0.0"
  }
}
